# 变更日志

本文件记录“TK试题题库”各安装版本中对用户可见的重要变化。版本号遵循 `主版本.次版本.修订版本` 格式。

## 0.1.83 - 2026-09-08

### 排版画布兼容与随机抽题增强

- 修复部分客户电脑因 WebView2 Worker 安全策略或公式尺寸兼容处理异常而无法初始化“排版与打印”画布的问题；非关键统计或公式兼容步骤失败时自动降级，画布仍可继续使用。
- 画布初始化失败时显示不包含题目内容的诊断代码，便于后续定位客户环境问题。
- 随机抽题单次上限与后端统一为 1000 道，输入 200 等超过 100 的题量不再被界面静默改成 100。
- 随机抽题支持同时选择多个学科；章节选项显示“学科 / 章节”，取消学科后自动移除不再属于当前范围的章节。

## 0.1.82 - 2026-09-07

### 排版自动同步题目篮

- 题目篮中的题目被新增、删除、替换或重新排序后，再次进入“排版与打印”会按最新内容自动重新生成版面，不再要求手动点击“重新生成”。
- 题目没有变化时继续恢复已保存的手工排版；自动重排时保留当前试卷模板和纸张尺寸设置。

## 0.1.81 - 2026-09-07

### 题目使用状态筛选统一

- 题库管理与随机抽题统一为“不限使用状态、从未使用过、本学期未使用、本月未使用过、本周未使用过、当天未使用过”六种筛选状态。
- “本学期未使用”固定按连续 180 天未使用判断；当天、本周和本月按电脑本地时间计算，本周从周一开始。
- 移除已不再生效的“近期未使用天数”设置项；旧数据字段继续保留，升级不会破坏现有设置。

## 0.1.80 - 2026-09-07

### 随机抽题使用状态筛选

- 随机抽题新增“不限使用状态、从未使用过、近期未使用过、本月未使用过、本周未使用过、当天未使用过”六种筛选状态。
- “近期未使用过”沿用系统设置中的近期未使用天数；当天、本周和本月按电脑本地时间计算，本周从周一开始。
- 候选数量统计和实际随机抽取使用同一筛选口径；关闭后重新打开窗口会保留使用状态，点击“重置条件”可恢复默认状态。

## 0.1.79 - 2026-09-07

### 随机抽题条件保留

- 在没有离开“选题组卷”页面时，关闭并重新打开随机抽题窗口会保留科目、章节、标签、匹配方式、抽取方式和题量。
- 重新打开窗口会根据右侧当前已选题目刷新候选数量；离开选题组卷页面后临时条件自动清空。
- 新增“重置条件”按钮，可主动恢复随机抽题的默认设置。

## 0.1.78 - 2026-09-06

### Word 模板导出修复

- 修复在打开 Word 导出窗口之前已经导入试卷模板时，导出窗口首次打开没有加载模板列表、因而错误提示需要先导入模板的问题。
- 模板列表读取期间显示加载状态，不再把尚未完成的读取过程误报为“暂无模板”。

## 0.1.77 - 2026-09-06

### 同步与编辑并发安全

- 云同步执行期间会锁定登录、退出和服务地址切换，并让本次同步的全部请求绑定同一账号快照，避免中途切换账号造成数据串号。
- 已存在的同步冲突会持续刷新到最新云端版本；选择“采用云端”时以本机已接收的最新云端状态为准，不再回放过期冲突快照。
- 等待科目、章节等依赖后重试的云端题目，会与最初记录的本机内容比较；本机未修改时自动补全，只有真实修改才升级为人工冲突。
- 题目富文本中的受管图片编号会同时映射关系记录、HTML 和编辑器 JSON，保证多设备归一图片后题干、选项、答案及解析都能正常显示。
- 题目草稿自动保存改为串行追赶最新输入；定时保存过程中继续编辑时，离开页面或关闭软件会等待最新内容真正写入。
- 试卷保存请求返回时若检测到等待期间仍有编辑，只合并数据库版本等保存元数据，不再用旧请求快照覆盖新标题、题目或排版。

## 0.1.76 - 2026-09-05

- 修复空白题目录入页因控件初始化事件被误判为“有未保存内容”，切换页面时错误提示保存草稿的问题；仅调整题型、科目、章节或标签但未录入题目内容时也不保存或提示，升级后会自动清理旧版本遗留的纯空白草稿。

### 云同步跨页依赖修复

- 修复首次下载大量题目时，题目先于其科目或章节跨页到达而被误报为人工同步冲突的问题。
- 外键依赖尚未到达的云端内容现在会持久等待并在后续分页或下次同步时自动补全，不再显示危险的“保留本机/采用云端”选择。
- 升级后会识别并自动重试 v0.1.75 已产生的同类记录；真正的本机与云端并发修改仍保留双方内容并要求人工确认。

## 0.1.75 - 2026-09-05

### 卸载数据与试用期修复

- 修复卸载时勾选“删除用户数据”后，默认位于“文档\\教师题库”的数据库、资源、模板、备份和导出仍被保留的问题。
- 卸载选项现在明确显示“删除本机题库、设置和登录信息（保留试用校验记录）”；仅删除软件登记并验证过的受管目录，数据根目录中的其他个人文件不会被误删。
- 设备标识、试用起始时间和可信时钟移到普通卸载不会清除的本机加密状态目录；从旧版升级时保留原试用起始时间，重新安装不再获得新的 15 天试用。
- 授权状态文件缺失、目录标记不匹配或题库目录含符号链接/重解析点时停止相关操作，不用“重新初始化”掩盖异常。
- Windows 安装器禁止直接降级到旧版本，降低通过回退客户端绕过新授权校验的风险。

## 0.1.74 - 2026-09-05

### 软件自动更新

- 软件启动后自动检查新版本，同一版本 24 小时内不会重复提醒；“关于软件”页面也可随时手动检查。
- 用户确认后下载并安装更新，显示下载进度；Windows 安装完成后自动重新启动软件。
- 更新安装包必须通过内置发布公钥校验，伪造、损坏或签名不匹配的文件不会被安装。
- 发布构建会同时生成更新签名和静态更新清单，供 `https://api.tktiku.cn/updates/` 分发。

## 0.1.73 - 2026-09-05

### 云同步兼容修复

- 修复旧分类 UUID 被云端归一后，本机上传陈旧删除记录时出现“云服务合并结果缺少本地实体”的问题。
- 云端吸收旧别名删除后，桌面端会清理陈旧同步状态并接续规范实体版本，不再把正常收敛误报为服务异常。

## 0.1.72 - 2026-09-05

### 多设备题库同步

- 同一账号在多台电脑新增的不同题目会全部保留；同名学科、同一学科下的同名章节、同名标签和相同图片会自动归一，不再制造重复分类。
- 首次同步前显示本机与云端题目数量；两边都有数据时默认执行非破坏性的智能合并，并继续保持一个本地题库只绑定一个教师账号。
- 本地业务数据继续使用本机 ID，通过云端身份映射同步依赖关系，避免批量改写题目、草稿和试卷外键。

### 大批量题目导入

- 修复审查完成后点击“导入本批题目”长时间持续转圈且缺少进度反馈的问题；导入过程现在显示当前阶段、完成数量和进度。
- 批量写入按每批最多 100 道执行，并支持停止后续批次；已经完成的数据库事务不会被中断，剩余题目会继续保存在审查草稿中。
- 多个完全相同的来源行准备覆盖同一道原题时只写入一次，其余按重复项跳过；同一原题对应不同来源内容时会在写入前阻止整批误覆盖。
- 覆盖原题改为原子事务并增加持久化幂等操作记录，超时确认或重试不会重复覆盖；写入、统计刷新和草稿保存均增加超时保护。

## 0.1.69 - 2026-09-04

### 教师账号与题目云同步

- 系统设置正式开放教师账号登录、注册、退出和立即同步入口，默认连接 `https://api.tktiku.cn`。
- 云同步覆盖题型、学科、章节、标签、题目及受管图片，所有服务端数据按教师账号隔离。
- 云授权与桌面离线授权相互独立；云授权到期只停止同步，不改变 15 天试用、宽限期和过期后的本地基础功能规则。
- 刷新令牌使用 Windows DPAPI 加密保存；同步遇到双方修改时保留冲突并由用户选择本机或云端版本。
- 首次同步必须明确确认本地题库与云账号的永久绑定；尚未绑定时不会在后台自动上传。
- 正式服务器已通过未授权拦截、令牌轮换、幂等写入、版本冲突和跨账号隔离验收。

## 0.1.68 - 2026-09-04

### Excel 导入修复

- 修复 Excel 已完成结构检查后，导入草稿文件名仍按 Word 规则要求 `.docx`，导致 `.xlsx` 无法进入审查页的问题。
- 批量导入草稿现在仅接受安全的 `.docx` 或 `.xlsx` 文件名，继续拒绝路径、宏工作簿和其他扩展名。

## 0.1.67 - 2026-09-04

### Excel 格式兼容

- 扩展 Excel 表头别名识别，支持“题干内容”“题目内容”“试题内容”“正确答案”“标准答案”“解题解析”等常见字段名称。
- 保留现有题型、选项、答案和解析内容，不把“见解析”等来源文本擅自改写为其他字段。
- 更新 Excel 分析器版本并增加第三方表头格式回归测试。

## 0.1.66 - 2026-09-03

### Excel 批量导入

- 新增 `.xlsx` 题库批量导入和可下载的标准模板，支持选择工作表并识别题型、学科、章节、题干、选项、答案、解析和标签。
- 当前软件导出的题库 Excel 可直接重新导入；合并选项列与 `选项A`、`选项B` 等分列格式均可识别。
- Excel 题目复用 Word 导入的四步审查、分类、重复检查和可恢复草稿，未知题型可映射或创建，未知标签可选择创建或忽略。
- Excel 首版只读取文字和值，不导入浮动图片、复杂表格和公式对象；增加文件大小、解压体积、部件、工作表、行列与单元格长度安全限制。

## 0.1.64 - 2026-08-29

### 性能与维护

- 随机抽题改由 SQLite 直接统计候选并抽取题目标识，只批量读取最终命中的完整题目，避免把候选题库全部传到前端。
- 题库列表批量读取选项、标签和图片资源引用，消除逐题重复查询；随机抽题统计会合并短时间内的连续筛选变化。
- 试卷组卷、导出、预览和画布编辑器改为按需加载，Element Plus 改为组件级引入，显著缩小首屏资源体积。
- 拆分随机抽题弹窗和 Word 导入富内容转换模块，移除已停用的旧自动选题前端代码，并同步更新技术说明。
- 增加随机抽题组件及真实 SQLite 抽取回归测试，覆盖标签匹配、已选题排除、候选统计和结果批量读取。

## 0.1.63 - 2026-08-29

### 选题组卷

- 精简随机抽题弹窗，删除顶部重复说明和底部实时统计卡片，题型列表改为独立滚动区域。
- 取消旧“一键组卷/自动组卷”入口和独立页面，由“随机抽题”作为右侧唯一的批量组卷入口。
- 从其他页面重新进入“选题组卷”时固定回到选题主页面；从历史试卷继续编辑时仍直接进入试卷编辑页面。
- 保留旧自动组卷试卷的数据兼容，历史试卷仍可正常打开和替换题目。

## 0.1.62 - 2026-08-29

### 随机抽题实时统计

- 修复未选择标签时候选题目一直显示“尚未统计”的问题。
- 标签改为可选；未选择标签时按当前学科和章节范围统计全部题目，选择标签后再按标签条件收窄范围。
- 学科、章节、标签、抽取方式、抽取数量及右侧已选题目变化时，实时更新候选数量和抽题预估。

## 0.1.61 - 2026-08-29

### 标签候选题库与分批抽题

- 新增“随机抽题”入口，可用一个或多个标签组成候选题库，并按“任一标签”或“全部标签”匹配。
- 支持按总题量或按题型随机抽取；每次结果累加到右侧，不清空已有题目，并自动排除已选题目。
- 标签、学科、章节、匹配方式和抽取数量变化时，实时更新可抽取数量、预计实际抽取、抽后剩余及各题型数量。
- 保留原有“一键组卷”，可继续一次生成整份试卷。

## 0.1.60 - 2026-08-28

### 文档录题与 Word 导入

- 文档式批量录入同时支持固定“题目：”标记和 `1.`、`2．`、`3、` 等数字题号，并默认从识别后的题干中移除题号。
- 旧录题模板和未完成草稿会自动启用兼容配置，无需重新创建模板。
- 修正逐题紧随选项的“答案：”“解析：”段落被错误追加到最后一个选项的问题。

## 0.1.59 - 2026-08-27

### 品牌名称

- 软件名称由旧产品名称统一修改为“TK试题题库”。
- 同步更新应用窗口、标题栏、安装包、快捷方式、备份与导出文件名、授权工具、Word/Excel 文档元数据以及全部项目说明。
- 保持应用标识符和数据结构不变，确保覆盖安装后继续使用原有本地题库数据。

## 0.1.58 - 2026-08-27

### 质量与维护

- 清理未接入的旧 Rust 领域模型和未使用接口，发布构建与 Clippy 严格检查均实现零警告。
- 前端测试排除 `.codex-tmp` 中的源码交接验证副本，避免同一批测试被重复统计。
- 更新发布验证口径，准确记录前端与 Rust 测试数量，并保留 Windows 文件锁问题的稳定验证方式。

### 产品范围

- 当前 Windows 正式交付明确为本地离线软件，不包含教师账号、云同步和网页端。
- 系统设置仅保留“桌面授权”入口；未发布的云账号与同步界面不再展示或在启动时读取状态。
- README、授权说明和发布说明统一云功能口径，避免对外宣传和售后承诺产生歧义。

## 0.1.57 - 2026-08-26

### 安全与交付

- 将构建链中的 `nanoid` 固定到 3.3.18、`postcss` 固定到 8.5.23，清理生产依赖审计报告中的已知漏洞。
- 将已交付的 0.1.56 源码快照固化为可验证 Git 提交和标签；新版源码交接同时提供 Git bundle、提交摘要和 SHA-256 文件清单。

### 文档与界面

- 统一试卷 Word、题库 Word 和题库 Excel 导出的真实能力说明，不再把已经接入的功能标为“正在开发”。
- 安装说明改为与 WebView2 联网引导安装方式一致，并避免在通用文档里硬编码过期版本号和测试数量。
- 明确 Word 导入不提供独立历史查询页，只保留当前可恢复的审查草稿。

## 0.1.56 - 2026-08-25

- 新选择、普通粘贴或从 WPS 带入的 PNG/JPEG 图片会写入软件受管资源目录。
- 图片按 SHA-256 内容摘要去重，相同图片只保存一份。
- 题目、普通草稿和文档式批量录入草稿会保存并恢复受管图片引用。

## 0.1.3 - 2026-07-17

- 修正网页外壳的中文语言、应用标题和题库图标。
- 软件名称统一调整为“TK试题题库”，并使用带透明背景的 TK 图标。
- 增加版本号同步与一致性检查，避免安装包、Rust 后端和前端显示版本不一致。
- 增加 ESLint 基础代码检查。

## 0.1.2 - 2026-07-17

### 新增

- 题库题目支持单题及多选右键快捷操作。
- 学科、章节支持新建题目、Word 导入、重命名、排序和安全删除等右键操作。
- Word 导入审查列表支持按题型、当前位置等方式快速批量勾选。

### 改进

- 从学科或章节进入题目录入、Word 导入时自动带入对应分类。
- Word 导入支持分批选择、分类和持续处理剩余题目。
- 所有题目继续保持不设置分值。

## 0.1.1 - 2026-07-15

- 改进不同学校试题 Word 文档的题型、题号和答案识别兼容性。
- 修正题库左侧学科及章节题目数量刷新。
- Word 导入审查阶段增加全选、全不选和批量分类。

## 0.1.0 - 2026-07-14

- 发布首个 Windows 离线安装版本。
- 提供题库、学科章节、Word 导入、组卷、模板、备份恢复和数据目录迁移等基础能力。
